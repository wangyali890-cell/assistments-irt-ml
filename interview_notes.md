# ASSISTments + IRT 学生作答预测面试准备

## 1. 这个项目解决什么问题？

项目根据学生到当前时刻为止的作答历史，以及当前题目的心理测量信息，预测学生下一次作答正确的概率。核心比较是普通 XGBoost 和加入 Rasch IRT 特征后的 XGBoost。

## 2. 为什么选择 ASSISTments？

ASSISTments 是公开的教育学习行为数据，包含学生、题目、正确性、交互顺序和 skill 等字段，既适合做行为特征，也适合展示 IRT 与机器学习的结合。

## 3. 为什么使用 XGBoost？

项目中的历史次数、正确率和 IRT 特征之间可能存在非线性和交互关系。XGBoost 对表格数据比较合适，训练成本可控，也便于输出特征重要性。

## 4. 为什么加入 IRT？

普通历史特征主要描述学生过去做得怎么样。IRT 提供了更明确的心理测量解释：学生能力、题目难度以及二者之间的匹配关系，可能补充行为特征没有直接表达的信息。

## 5. Rasch 模型是什么？

Rasch 是一种 1PL IRT 模型，用学生能力减去题目难度的差值预测答对概率：(P(correct)=\sigma(\theta-b))。它只估计能力和难度，不额外估计题目的区分度或猜测参数。

## 6. (	heta) 代表什么？

(	heta) 是学生能力参数。在题目难度固定时，(	heta) 越高，模型预测答对的概率越高。本项目的 (	heta) 只由学生训练阶段的作答记录估计。

## 7. (b) 代表什么？

(b) 是题目难度参数。在学生能力固定时，(b) 越高，题目越难，预测正确概率越低。本项目的题目难度只使用训练集估计。

## 8. 为什么不能直接使用学生总体正确率？

如果总体正确率是用全量数据计算的，就会把当前答案和未来答案带入当前特征。这会让离线指标虚高，不能模拟真实的在线预测场景。本项目使用当前交互之前的累计正确率。

## 9. 什么是数据泄漏？

数据泄漏是指训练或预测特征包含了在实际预测时不可能获得的信息，例如当前目标答案、未来作答结果，或者使用全量数据拟合出的参数。

## 10. 为什么采用 temporal split？

预测的是学生未来表现，所以训练数据必须早于验证数据，验证数据必须早于测试数据。学生内时间切分比随机切分更接近实际使用场景，也能减少未来信息进入训练集的风险。

## 11. 为什么 IRT 只能在 training data 上拟合？

如果用 Validation 或 Test 的答案重新估计能力和难度，就把评估阶段的目标信息用于特征生成，比较结果会失真。因此本项目只用 Train 拟合一次，然后固定参数。

## 12. 为什么 Validation/Test 阶段不能用未来答案更新 (	heta)？

本项目要评价固定训练模型对后续交互的预测能力。如果在评估阶段根据未来答案更新 (	heta)，就不再是同一个固定模型，也无法判断模型是否真正能够泛化到未来交互。

## 13. IRT 特征为什么可能帮助 XGBoost？

历史正确率可以反映表现，但不一定区分是学生能力高还是题目较容易。IRT 将学生和题目放到同一个能力-难度尺度上，`theta_minus_difficulty` 能直接表达当前题目对该学生的相对挑战程度。

## 14. 如果 IRT 没有提升 AUC，怎么解释？

这并不代表 IRT 没有意义。可能是历史行为特征已经包含了大部分能力信息，也可能是 Rasch 的单一能力假设过于简单，或者数据中的题目难度变化已经被 XGBoost 学到了。我会如实报告结果，而不是预设 IRT 一定提升。

## 15. 为什么不能只看 Accuracy？

Accuracy 依赖阈值，而且在类别不平衡时可能被多数类主导。这个项目输出的是概率，所以还需要 AUC 衡量排序能力，LogLoss 和 Brier Score 衡量概率质量与校准表现。

## 16. AUC、LogLoss 和 Brier Score 分别反映什么？

AUC 衡量模型把正确样本排在错误样本前面的能力；LogLoss 对自信但错误的概率惩罚较大；Brier Score 衡量预测概率与真实 0/1 结果之间的均方误差。AUC 越高越好，后两者越低越好。

## 17. XGBoost 与 Logistic Regression 的差异是什么？

Logistic Regression 是线性、可解释的基线，适合检查简单关系。XGBoost 通过树模型捕捉非线性和特征交互，更适合表格数据，但解释通常依赖特征重要性等辅助工具。

## 18. 本项目最重要的技术难点是什么？

最重要的是确保时间因果性：历史特征不能包含当前答案，IRT 不能使用 Validation/Test 标签重新拟合，训练、验证、测试必须按学生内时间顺序排列。

## 19. 本项目有什么局限？

项目主要评价已有学习历史的学生，不能直接说明对新学生有效；IRT 使用 Rasch/1PL，表达能力有限；测试阶段固定能力参数，也没有建模动态知识状态和遗忘过程。

## 20. 如果再给一周时间，你会增加什么？

我会先做严格的新学生 cold-start 评估，再尝试 2PL 或动态 IRT，并与 Bayesian Knowledge Tracing 做对照。如果模型效果和数据规模允许，再考虑 LSTM 或 Transformer，而不是直接扩大模型复杂度。
